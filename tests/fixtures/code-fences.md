# Code fences

```python
x = [[1, 2], [3, 4]]  # brackets, not wikilinks
```

```
[[also/not|a link]] inside a plain fence
```

Inline code `[[not/a/link]]` stays literal, beside a real
[[topics/alpha|link]].
